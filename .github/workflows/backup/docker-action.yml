name: Docker action

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3

    - name: Run Login
      uses: docker/login-action@v3.3.0
      with:
        name: 'GitHub User'
          
